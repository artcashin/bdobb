// Placeholder shell — Task 17 replaces this with the real help browser UI
// (nav tree, article view, search) built on src/help/generated/*.
export default function HelpApp() {
  return <div className="help-app">Loading…</div>;
}
